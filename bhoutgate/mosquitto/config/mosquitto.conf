# Basic configuration
listener 1883
allow_anonymous true

# Log configuration
log_dest stdout
log_type all

# TLS configuration (commented out for now, will be enabled later)
# listener 8883
# cafile /path/to/ca.crt
# certfile /path/to/server.crt
# keyfile /path/to/server.key
# require_certificate false

# Security settings (will be enabled later)
# password_file /path/to/password_file
# acl_file /path/to/acl_file 